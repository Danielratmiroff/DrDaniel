// General constants

import { indexes } from "../temporary.json";
import { Pill } from "../types/types";

export const pillStartPoint: Pill = {
  col: indexes[indexes.length / 2 - 1], // align in the center column
  row: 0,
};
