import { FC, useEffect, KeyboardEvent, useState, createRef } from "react";
import { indexes } from "../temporary.json";
import { getCol, getNextRow, isNextRowValid } from "../utils/NodePosition";

export const usePillDrop = (setPill) => { };
