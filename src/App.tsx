import React, { FC, KeyboardEvent, useRef, useState } from "react";
import Grid from "./components/Grid/Grid";

const App: FC = () => {
  return (
    <>
      <Grid />
    </>
  );
};

export default App;
